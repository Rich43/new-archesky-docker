apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: ./kompose convert
    kompose.version: 1.21.0 (992df58d8)
  labels:
    io.kompose.service: keycloak
  name: keycloak
spec:
  ports:
  - name: "8180"
    port: 8180
    targetPort: 8080
  selector:
    io.kompose.service: keycloak
